/**
 * Module 14 — Offline Circulation & Freelancers
 * react-query hook (useQuery/useMutation) এখানে লিখবে -- api/circulation.ts-এর ফাংশন wrap করে।
 *
 * STUB — এখানে এখনো কোনো hook নেই।
 *
 * উদাহরণ প্যাটার্ন (দেখো hooks/useUser.ts):
 *   export function useCirculation(id: number) {
 *     return useQuery({ queryKey: ["circulation", id], queryFn: () => getX(id) });
 *   }
 */

// TODO: Circulation মডিউলের hook লেখো
export {};
