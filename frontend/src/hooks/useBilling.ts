/**
 * Module 8 — Monetization, Paywall & B2B
 * react-query hook (useQuery/useMutation) এখানে লিখবে -- api/billing.ts-এর ফাংশন wrap করে।
 *
 * STUB — এখানে এখনো কোনো hook নেই।
 *
 * উদাহরণ প্যাটার্ন (দেখো hooks/useUser.ts):
 *   export function useBilling(id: number) {
 *     return useQuery({ queryKey: ["billing", id], queryFn: () => getX(id) });
 *   }
 */

// TODO: Billing মডিউলের hook লেখো
export {};
