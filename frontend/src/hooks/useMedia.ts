/**
 * Module 4 — Digital Asset Management & AI
 * react-query hook (useQuery/useMutation) এখানে লিখবে -- api/media.ts-এর ফাংশন wrap করে।
 *
 * STUB — এখানে এখনো কোনো hook নেই।
 *
 * উদাহরণ প্যাটার্ন (দেখো hooks/useUser.ts):
 *   export function useMedia(id: number) {
 *     return useQuery({ queryKey: ["media", id], queryFn: () => getX(id) });
 *   }
 */

// TODO: Media মডিউলের hook লেখো
export {};
