/**
 * Module 11 — Multimedia & Live News
 * react-query hook (useQuery/useMutation) এখানে লিখবে -- api/multimedia.ts-এর ফাংশন wrap করে।
 *
 * STUB — এখানে এখনো কোনো hook নেই।
 *
 * উদাহরণ প্যাটার্ন (দেখো hooks/useUser.ts):
 *   export function useMultimedia(id: number) {
 *     return useQuery({ queryKey: ["multimedia", id], queryFn: () => getX(id) });
 *   }
 */

// TODO: Multimedia মডিউলের hook লেখো
export {};
