/**
 * Module 12 — Marketing, Notifications & Interactive
 * react-query hook (useQuery/useMutation) এখানে লিখবে -- api/marketing.ts-এর ফাংশন wrap করে।
 *
 * STUB — এখানে এখনো কোনো hook নেই।
 *
 * উদাহরণ প্যাটার্ন (দেখো hooks/useUser.ts):
 *   export function useMarketing(id: number) {
 *     return useQuery({ queryKey: ["marketing", id], queryFn: () => getX(id) });
 *   }
 */

// TODO: Marketing মডিউলের hook লেখো
export {};
