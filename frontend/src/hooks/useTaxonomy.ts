/**
 * Module 5 — Taxonomy & Discovery
 * react-query hook (useQuery/useMutation) এখানে লিখবে -- api/taxonomy.ts-এর ফাংশন wrap করে।
 *
 * STUB — এখানে এখনো কোনো hook নেই।
 *
 * উদাহরণ প্যাটার্ন (দেখো hooks/useUser.ts):
 *   export function useTaxonomy(id: number) {
 *     return useQuery({ queryKey: ["taxonomy", id], queryFn: () => getX(id) });
 *   }
 */

// TODO: Taxonomy মডিউলের hook লেখো
export {};
