/**
 * Module 13 — Ads, Commerce & Classifieds
 * react-query hook (useQuery/useMutation) এখানে লিখবে -- api/ads.ts-এর ফাংশন wrap করে।
 *
 * STUB — এখানে এখনো কোনো hook নেই।
 *
 * উদাহরণ প্যাটার্ন (দেখো hooks/useUser.ts):
 *   export function useAds(id: number) {
 *     return useQuery({ queryKey: ["ads", id], queryFn: () => getX(id) });
 *   }
 */

// TODO: Ads মডিউলের hook লেখো
export {};
