/**
 * Module 17 — A/B Testing & Feature Flags
 * react-query hook (useQuery/useMutation) এখানে লিখবে -- api/experiments.ts-এর ফাংশন wrap করে।
 *
 * STUB — এখানে এখনো কোনো hook নেই।
 *
 * উদাহরণ প্যাটার্ন (দেখো hooks/useUser.ts):
 *   export function useExperiments(id: number) {
 *     return useQuery({ queryKey: ["experiments", id], queryFn: () => getX(id) });
 *   }
 */

// TODO: Experiments মডিউলের hook লেখো
export {};
