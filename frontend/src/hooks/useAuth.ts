/**
 * Module 2 — Identity & Access Management
 * react-query hook (useQuery/useMutation) এখানে লিখবে -- api/auth.ts-এর ফাংশন wrap করে।
 *
 * STUB — এখানে এখনো কোনো hook নেই।
 *
 * উদাহরণ প্যাটার্ন (দেখো hooks/useUser.ts):
 *   export function useAuth(id: number) {
 *     return useQuery({ queryKey: ["auth", id], queryFn: () => getX(id) });
 *   }
 */

// TODO: Auth মডিউলের hook লেখো
export {};
