/**
 * Module 18 — Editorial Workflow — Newsroom CMS
 * react-query hook (useQuery/useMutation) এখানে লিখবে -- api/newsroom.ts-এর ফাংশন wrap করে।
 *
 * STUB — এখানে এখনো কোনো hook নেই।
 *
 * উদাহরণ প্যাটার্ন (দেখো hooks/useUser.ts):
 *   export function useNewsroom(id: number) {
 *     return useQuery({ queryKey: ["newsroom", id], queryFn: () => getX(id) });
 *   }
 */

// TODO: Newsroom মডিউলের hook লেখো
export {};
