/**
 * Module 6 — Core Editorial Engine
 * react-query hook (useQuery/useMutation) এখানে লিখবে -- api/articles.ts-এর ফাংশন wrap করে।
 *
 * STUB — এখানে এখনো কোনো hook নেই।
 *
 * উদাহরণ প্যাটার্ন (দেখো hooks/useUser.ts):
 *   export function useArticles(id: number) {
 *     return useQuery({ queryKey: ["articles", id], queryFn: () => getX(id) });
 *   }
 */

// TODO: Articles মডিউলের hook লেখো
export {};
