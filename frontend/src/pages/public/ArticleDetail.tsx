/**
 * ArticleDetail — একটা নির্দিষ্ট আর্টিকেলের পূর্ণ পেজ — ব্লক-এডিটর কনটেন্ট রেন্ডার, কমেন্ট, related articles
 * (Module 6)
 *
 * STUB — এখানে এখনো কোনো UI নেই।
 *
 * লেখা শেষ হলে src/App.tsx-এ route যোগ করতে ভুলো না:
 *   <Route path="..." element={<ArticleDetailPage />} />
 */
export default function ArticleDetailPage() {
  return (
    <div>
      <h2>ArticleDetail</h2>
      <p>TODO: এই পেজ ইমপ্লিমেন্ট করো।</p>
    </div>
  );
}
