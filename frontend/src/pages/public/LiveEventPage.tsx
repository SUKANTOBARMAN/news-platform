/**
 * LiveEventPage — লাইভ ইভেন্ট কভারেজ পেজ (রিয়েল-টাইম আপডেট)
 * (Module 11)
 *
 * STUB — এখানে এখনো কোনো UI নেই।
 *
 * লেখা শেষ হলে src/App.tsx-এ route যোগ করতে ভুলো না:
 *   <Route path="..." element={<LiveEventPagePage />} />
 */
export default function LiveEventPagePage() {
  return (
    <div>
      <h2>LiveEventPage</h2>
      <p>TODO: এই পেজ ইমপ্লিমেন্ট করো।</p>
    </div>
  );
}
