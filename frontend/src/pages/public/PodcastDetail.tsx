/**
 * PodcastDetail — একটা পডকাস্ট শো ও তার এপিসোড লিস্ট
 * (Module 11)
 *
 * STUB — এখানে এখনো কোনো UI নেই।
 *
 * লেখা শেষ হলে src/App.tsx-এ route যোগ করতে ভুলো না:
 *   <Route path="..." element={<PodcastDetailPage />} />
 */
export default function PodcastDetailPage() {
  return (
    <div>
      <h2>PodcastDetail</h2>
      <p>TODO: এই পেজ ইমপ্লিমেন্ট করো।</p>
    </div>
  );
}
