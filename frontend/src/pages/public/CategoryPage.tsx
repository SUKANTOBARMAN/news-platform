/**
 * CategoryPage — একটা ক্যাটাগরি/সাব-ক্যাটাগরির আর্টিকেল লিস্টিং (cursor pagination সহ)
 * (Module 5)
 *
 * STUB — এখানে এখনো কোনো UI নেই।
 *
 * লেখা শেষ হলে src/App.tsx-এ route যোগ করতে ভুলো না:
 *   <Route path="..." element={<CategoryPagePage />} />
 */
export default function CategoryPagePage() {
  return (
    <div>
      <h2>CategoryPage</h2>
      <p>TODO: এই পেজ ইমপ্লিমেন্ট করো।</p>
    </div>
  );
}
