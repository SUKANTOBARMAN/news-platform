/**
 * SearchResults — সার্চ রেজাল্ট পেজ (Meilisearch/fallback ব্যবহার করে)
 * (Module 4)
 *
 * STUB — এখানে এখনো কোনো UI নেই।
 *
 * লেখা শেষ হলে src/App.tsx-এ route যোগ করতে ভুলো না:
 *   <Route path="..." element={<SearchResultsPage />} />
 */
export default function SearchResultsPage() {
  return (
    <div>
      <h2>SearchResults</h2>
      <p>TODO: এই পেজ ইমপ্লিমেন্ট করো।</p>
    </div>
  );
}
