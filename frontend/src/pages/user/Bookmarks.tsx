/**
 * Bookmarks — ইউজারের সংরক্ষিত আর্টিকেলের তালিকা
 * (Module 9)
 *
 * STUB — এখানে এখনো কোনো UI নেই।
 *
 * লেখা শেষ হলে src/App.tsx-এ route যোগ করতে ভুলো না:
 *   <Route path="..." element={<BookmarksPage />} />
 */
export default function BookmarksPage() {
  return (
    <div>
      <h2>Bookmarks</h2>
      <p>TODO: এই পেজ ইমপ্লিমেন্ট করো।</p>
    </div>
  );
}
