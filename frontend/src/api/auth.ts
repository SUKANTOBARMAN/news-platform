/**
 * Module 2 — Identity & Access Management
 * axios দিয়ে backend /auth endpoint-এ কল করার ফাংশন এখানে লিখবে।
 *
 * STUB — এখানে এখনো কোনো ইমপ্লিমেন্টেশন নেই।
// নোট: নোট: users.ts ইতিমধ্যে আছে (createUser, getUser) — এখানে login/OTP/token রিফ্রেশ যোগ করবে
 * উদাহরণ প্যাটার্ন (দেখো api/users.ts):
 *   import { apiClient } from "@/api/client";
 *   export async function getX(id: number) { ... }
 */
// import { apiClient } from "@/api/client"; // TODO: ফাংশন লেখার সময় আনকমেন্ট করো

// TODO: Auth মডিউলের API ফাংশন লেখো
