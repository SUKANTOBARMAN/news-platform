"""
Search (cross-cutting)
Role: public (লগইন ছাড়াই অ্যাক্সেসযোগ্য — যেকোনো ভিজিটর/পাঠক (Guest))

STUB — এখানে এখনো কোনো ইমপ্লিমেন্টেশন নেই।
নোট: Meilisearch/FULLTEXT fallback সার্চ endpoint

পূর্ণ কলাম-বিবরণের জন্য দেখো docs/project_documentation_redesigned.pdf
(সংশ্লিষ্ট মডিউল নাম্বার)।
"""
from fastapi import APIRouter

router = APIRouter()

# TODO: এখানে search সংক্রান্ত endpoint লেখো
